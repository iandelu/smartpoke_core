package com.smartpoke.api.common.utils;

import com.smartpoke.api.feature.recipe.model.RecipeProduct;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class IngredientPorcessorTest {

    /* ingredientProcessor = new IngredientProcessor();


    @Test
    public void unitOfMeasureServiceTest() {
        String ingredientText = "1 kilo de harina";

        RecipeProduct recipeProduct = ingredientProcessor.ingredientTextToProduct(ingredientText);

        assertEquals(recipeProduct.getAmount(), 1);
        assertEquals(recipeProduct.getUnitOfMeasure().getName(), "kg");
        assertEquals(recipeProduct.getProduct().getName(),  "de harina");
    }*/
}
